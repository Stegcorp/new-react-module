import React, {useState} from 'react';

const Albams = () => {
    const [ambams,setAlbams] = useState([]);
    return (
        <div>
            aaaaa
        </div>

    );
};

export default Albams;
