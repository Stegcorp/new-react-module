// const baseURL = process.env.REACT_APP_API
const baseURL = 'https://jsonplaceholder.typicode.com'


const urls = {
    todos: '/todos',
    albams: '/albums',
    comments:'/comments',
    posts:'/posts'
}

export {
    baseURL,
    urls
}
